<nav class="navbar fixed-top navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Akd</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% for route_name, route_path in routes %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path(route_path) }}">{{ route_name }}</a>
                    </li>
                {% endfor %}
            </ul>
            <ul class="navbar-nav ms-auto">
                {{ include('nav/theme.html.twig') }}

                {# Dropdown para trocar role #}
                {% if switchableRoles|length > 0 %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="roleDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            {{ roleNames[activeRole]|default(activeRole)  }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="roleDropdown">
                            {% for role in switchableRoles %}
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_switch_role', {'role': role}) }}">
                                        {{ roleNames[role]|default(role) }}
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" data-bs-toggle="tooltip"
                       data-bs-placement="left" title="Logout">
                        {{ ux_icon('material-symbols:logout') }}
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
