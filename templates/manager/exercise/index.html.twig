{% extends 'manager/base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('manager_exercise') }}
{% endblock %}

{% block title %}
    <h1>Exercícios Cadastrados</h1>
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 mx-auto mt-5">
                {# Barra superior #}
                <div class="d-flex justify-content-between align-items-center mb-3 mx-3">

                    {# Pesquisa #}
                    <div class="input-group w-50">
                    <span class="input-group-text">
                        {{ ux_icon('material-symbols:search') }}
                    </span>
                        <input
                            type="text"
                            class="form-control"
                            placeholder="Pesquisar exercícios..."
                            id="search-exercise"
                        >
                    </div>

                    {# Botão novo #}
                    <button class="btn btn-sm btn-secondary open-exercise-modal"
                        data-bs-toggle="modal"
                        data-bs-target="#exerciseModal"
                        data-url = "{{ path(route('MANAGER_EXERCISE_FORM_NEW')) }}"
                    >
                        {{ ux_icon('material-symbols:add') }}
                        Novo
                    </button>

                </div>
                <div class="table-responsive shadow rounded bg-body p-3">
                    <table class="table table-striped table-hover align-middle text-center table-bordered">
                        <thead class="table">
                        <tr>
                            <th class="w-25">Nome</th>
                            <th class="w-50">Descrição</th>
                            <th class="w-25">Ações</th>
                        </tr>
                        </thead>
                        <tbody id="exercise-table">
                        {% for exec in exercises %}
                            <tr>
                                <td class="fw-bold">
                                    {{ exec.name|title }}
                                </td>

                                <td>
                                    {{ exec.description }}
                                </td>

                                <td class="d-flex justify-content-center">
                                    {# Botão Edit #}
                                    <button class="btn btn-sm btn-outline-primary mx-3 open-exercise-modal"
                                    data-bs-toggle="modal"
                                    data-bs-target="#exerciseModal"
                                    data-url = "{{ path(route('MANAGER_EXERCISE_FORM_EDIT'), {id : exec.id}) }}"
                                    >
                                        {{ ux_icon('material-symbols:edit') }}
                                    </button>

                                    {# Botão Delete #}
                                    <form class="delete-form"
                                        method="post"
                                        action="{{ path(route('MANAGER_EXERCISE_DELETE'), { id: exec.id }) }}"
                                    >
                                        <button class="btn btn-sm btn-outline-danger"
                                                type="submit"
                                        >
                                            {{ ux_icon('material-symbols:delete') }}
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3" class="text-center text-muted">
                                    Nenhum exercício encontrado.
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>

    <div class="modal fade" id="exerciseModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title">Exercício</h5>

                    <button type="button"
                            class="btn-close"
                            data-bs-dismiss="modal">
                    </button>
                </div>

                <div class="modal-body" id="modal-body-content">

                    {# formulário será carregado aqui via AJAX #}

                </div>

            </div>
        </div>
    </div>

{% endblock %}


